import React from 'react';
import PropTypes from 'prop-types';

const TableUser = () => {
    return (
        <div>
            
        </div>
    );
};

TableUser.propTypes = {};

export default TableUser;