import React from 'react';
// import PropTypes from 'prop-types';

const FormUser = () => {
    return (
        <div>
            
        </div>
    );
};

FormUser.propTypes = {};

export default FormUser;