import React from 'react';
import PropTypes from 'prop-types';

const TablePictures = () => {
    return (
        <div>
            
        </div>
    );
};

TablePictures.propTypes = {};

export default TablePictures;