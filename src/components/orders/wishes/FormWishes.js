import React from 'react';
import PropTypes from 'prop-types';

const FormWishes = () => {
    return (
        <div>
            
        </div>
    );
};

FormWishes.propTypes = {};

export default FormWishes;