import React from 'react';
import PropTypes from 'prop-types';

const TableWishes = () => {
    return (
        <div>
            
        </div>
    );
};

TableWishes.propTypes = {};

export default TableWishes;